<script type="text/javascript" src="/js/DataTableUsuarios.js" ></script>
<div class="row-fluid">
    <div class="span8">
        <h2>Edici&oacute;n de Usuarios</h2>
    </div>
    <div class="span4">
        <div class="input-append">
            <input type="text" id="txtSerch" class="span10">
            <button id="serch" class="btn" type="button">Buscar</button>
        </div>
    </div>
</div>
    <div id="titleTable" style="width: 100%;background-color: #0b67cd;color: #FFFFFF;font: 600 larger sans-serif;height: 30px;vertical-align: middle" >
        &nbsp;
    </div>
    <table class="table table-hover text-center" id="listUser" cellpadding="10" style="width: 100%">
        <thead style="background-color: #2D89EF;color: #FFFFFF">
            <tr>
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Correo</th>
                <th>Perfil</th>
                <th>Permisos</th>
                <th colspan="2"></th>
            </tr>
        </thead>
    </table>
    <div id="dialog-edit" title="Editar Usuario">
        <?php
$formLabel = $this->plugin('formLabel');
$form = $this->formEdit;
$form->prepare();
echo $this->form()->openTag($form);
?>
<input type="hidden" name="Id">
<?php
$nombre = $form->get('nombre');
echo $formLabel->openTag(). $nombre->getOption('label').$formLabel->closeTag().$this->formInput($nombre);
$apellido = $form->get('apellido');
echo $formLabel->openTag(). $apellido->getOption('label').$formLabel->closeTag().$this->formInput($apellido);
$correo = $form->get('correo');
echo $formLabel->openTag(). $correo->getOption('label').$formLabel->closeTag().$this->formInput($correo);
$perfil = $form->get('perfil');
echo $formLabel->openTag(). $perfil->getOption('label').$formLabel->closeTag().$this->formSelect($perfil);
$permiso = $form->get('permisos');
echo $formLabel->openTag(). $permiso->getOption('label').$formLabel->closeTag().$this->formMultiCheckbox($permiso);
?>
    </div>
    <div id="deleteDiv" title="¿Desea borrar el usuario?">
        <p><span style="float:left; margin:0 7px 20px 0;" class="icon-warning-sign"></span>&iquest;Esta seguro que desea eliminar al usuario?</p>
    </div>
